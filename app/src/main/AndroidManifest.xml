<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.victorovdv"
    android:versionCode="0"
    android:versionName="Alpha 0.1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/game"
        android:label="Best app"
        android:roundIcon="@mipmap/game_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VictorovDV">
        <activity
            android:name=".CongratuationActivity"
            android:exported="false">
            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>